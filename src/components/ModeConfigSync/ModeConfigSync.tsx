import React from 'react';
import { useModeConfigSync } from '../../hooks/useModeConfigSync';

export function ModeConfigSync() {
  useModeConfigSync();
  return null;
}

export default ModeConfigSync;
